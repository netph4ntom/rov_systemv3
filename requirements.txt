# requirements.txt — Project ROV Vision
# Install: pip install -r requirements.txt

# ── Computer Vision (Gunakan versi sistem apt di Raspberry Pi 5) ──
# Di Raspberry Pi 5, gunakan: sudo apt install python3-opencv python3-numpy python3-gi
# Dan aktifkan `include-system-site-packages = true` di `.venv/pyvenv.cfg`
# (Dikomparasikan di bawah hanya untuk referensi/pengembangan lokal di Windows)
# opencv-contrib-python>=4.8.0,<5.0.0
# numpy>=1.24.0,<2.0.0
pyzbar                      # QR code / barcode scanner

# ── Web Server / API ───────────────────────────
fastapi>=0.100.0             # Async REST API framework
uvicorn[standard]>=0.22.0    # ASGI server for FastAPI/Socket.IO
python-socketio>=5.10.0      # Socket.IO backend for python
python-engineio>=4.9.0       # Engine.IO transport layer

# ── Async / Networking ─────────────────────────
pyzmq>=25.0.0                # ZeroMQ for high-performance IPC


# ── Utilities ─────────────────────────────────
pymavlink>=2.4.40              # MAVLink komunikasi dengan UART

# ── Utilities ─────────────────────────────────
python-dotenv>=1.0.0        # load .env file (opsional, untuk konfigurasi via env var)

# ── WebRTC ─────────────────────────────────────
aiortc>=1.6.0               # WebRTC streaming

# ── Failsafe & Watchdog ─────────────────────────────────────
psutil>=5.9.0